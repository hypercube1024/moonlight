@Include(name = "hello.h")
namespace com.moonlight.test;

import com.moonlight.test.msg.*;

struct Hello<T> extends Request<string> {
    T data;
    string name;
    int age;
    required Hero hero;
}

struct Answer<Foo<T>> extends Response<string> {
    optional T data;
    long time;
}

@Abc
@Xyz
annotation Realm {
    int x = 0;
    int y;
    string[] names = ["abc", "bcd" ];
    boolean[] ssss = [false, true];
}

@Realm(x = 4, y = 3)
enum Status {
    int open = 1;
    int close = 0;
}

enum Msg {
    string error = "resource not found";
    string success = "";
}

com.test.@Realm()
interface HelloService {
    Answer<string> hello(Hello<string> param);
}